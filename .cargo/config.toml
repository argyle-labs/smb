# The orca repo is private. Use the git CLI (which inherits the operator's
# authenticated gh / ssh credentials) so cargo can resolve the `plugin-toolkit`
# git source pinned in Cargo.toml.
[net]
git-fetch-with-cli = true
